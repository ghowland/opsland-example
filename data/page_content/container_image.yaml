name: image

tags:
  - basic
  - image


# Include content into this widget.  Each include key is a new place to template all the includes, which we can limit to 1 or have more
include: {}

style:
  - border-color: color_theme_or_override
  - border-width: int
  - size: int
  - rounded: int

data:
  - image: image
