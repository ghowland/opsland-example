name: list

tags:
  - list
  - container

# Include content into this widget.  Each include key is a new place to template all the includes, which we can limit to 1 or have more
include:
  default:
    tags:
      - list_item
    min: 1
    max: null

style:
  - margin: int
  - icon: icon
  - style: int
    info: "enum for style: 0=normal, 1=timeline, 2=etc"

data: []
